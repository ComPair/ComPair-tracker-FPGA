This is the dacctrl application for writing values to the DAC121S101 dac.

To build this, you will need to create a new c++ application, and rename
main.cc to dacctrl.cpp, then copy this source into the sdk application's
source directory.
